<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="EvaluationRepository">

  <select id="select" resultType="map">
    SELECT * FROM EVALUATION WHERE Evaluation_ID = #{value}
  </select>

  <select id="selectAll" resultType="map">
    SELECT * FROM EVALUATION
  </select>

  <select id="selectLecture_id" resultType="map">
    SELECT * FROM EVALUATION WHERE LECTURE_ID = #{value}
  </select>

  <delete id="delete" parameterType="int">
    DELETE FROM EVALUATION WHERE EVALUATION_ID = #{value}
  </delete>

  <insert id="insert" parameterType="map">
    INSERT INTO EVALUATION (member_id, lecture_id, method, task, exam, comment, score)
    VALUES (#{member_id},#{lecture_id},#{method},#{task},#{exam},#{comment},#{score})
  </insert>

  <update id="update" parameterType="map">
    UPDATE EVALUATION
    SET
    method = #{method},
    task = #{task},
    exam = #{exam},
    comment = #{comment},
    score = #{score}
    WHERE EVALUATION_ID = #{evaluation_id}
  </update>

  <update id="updateLike" parameterType="map">
    UPDATE LECTURE
    SET
    likes = likes + 1
    WHERE LECTURE_ID = #{lecture_id}
  </update>

  <update id="updateDislike" parameterType="map">
    UPDATE LECTURE
    SET
    likes = dislike + 1
    WHERE LECTURE_ID = #{lecture_id}
  </update>

  <update id="updateReport" parameterType="map">
    UPDATE LECTURE
    SET
    likes = report + 1
    WHERE LECTURE_ID = #{lecture_id}
  </update>

</mapper>