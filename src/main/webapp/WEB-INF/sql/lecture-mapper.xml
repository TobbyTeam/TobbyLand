<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="LectureRepository">

  <select id="select" resultType="map">
    SELECT * FROM LECTURE WHERE LECTURE_ID = #{value}
  </select>

  <select id="selectAll" resultType="map">
    <![CDATA[  SELECT * FROM LECTURE WHERE likes < 10 ]]>
  </select>

  <select id="selectName" resultType="map">
    SELECT * FROM LECTURE WHERE likes <![CDATA[ >= ]]> 10 AND LECTURE_NAME LIKE CONCAT('%', #{value}, '%')
  </select>

  <select id="selectDept" resultType="map">
    SELECT * FROM LECTURE WHERE DEPT LIKE CONCAT('%', #{value}, '%')
  </select>

  <select id="selectProf" resultType="map">
    SELECT * FROM LECTURE WHERE PROF LIKE CONCAT('%', #{value}, '%')
  </select>

  <delete id="delete" parameterType="int">
    DELETE FROM LECTURE WHERE LECTURE_ID = #{value}
  </delete>

  <insert id="insert" parameterType="map">
    INSERT INTO LECTURE (member_id, lecture_name, dept, prof)
    VALUES (#{member_id},#{lecture_name},#{dept},#{prof})
  </insert>

  <update id="update" parameterType="map">
    UPDATE LECTURE
    SET
    lecture_name = #{lecture_name},
    dept = #{dept},
    prof = #{prof}
    WHERE LECTURE_ID = #{lecture_id}
  </update>

  <update id="updateLike" parameterType="map">
    UPDATE LECTURE
    SET
    likes = likes + 1
    WHERE LECTURE_ID = #{lecture_id}
  </update>

</mapper>